(ns backend.game
  (:require [backend.hangman :refer [guess-word reveal-letter]]))


(defn move
  "Reveal one letter or try to guess immediately"
  [])

;TODO: for the parallel computation create one string for every game
(defn- main
  [args]
  (println ))
